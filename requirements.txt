fastapi==0.141.1
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
pyjwt==2.13.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
slowapi==0.1.9
requests==2.33.0
psycopg2-binary==2.9.9
gunicorn==23.0.0
anthropic==0.39.0

# dev/test only
pytest==9.0.3
httpx==0.27.2

# NOTE: Bandit, Semgrep, and pip-audit are intentionally NOT listed
# here. Semgrep pulls in an MCP client that needs a newer Pydantic
# than this app is pinned to (2.9.2) — installing security tooling
# in the same environment as the app created a real, unresolvable
# dependency conflict during development. That's the standard reason
# to keep SAST/SCA tools isolated from application dependencies
# rather than fight the resolver: see requirements-security.txt and
# .github/workflows/security.yml, which installs them via pipx
# (isolated per-tool virtualenvs) instead.
